export function Page({ eyebrow, title, subtitle, children, action }: { eyebrow:string; title:string; subtitle?:string; children:React.ReactNode; action?:React.ReactNode }) { return <section className="content"><div style={{display:'flex',justifyContent:'space-between',alignItems:'start',gap:20}}><div><div className="eyebrow">{eyebrow}</div><h1 className="title">{title}</h1>{subtitle&&<p className="subtitle">{subtitle}</p>}</div>{action}</div>{children}</section> }
export function Stat({label,value,detail}:{label:string;value:string;detail:string}){return <div className="card"><div className="metric-label">{label}</div><div className="metric">{value}</div><div className="muted" style={{fontSize:12}}>{detail}</div></div>}
export function EmptyState({title,body}:{title:string;body:string}){return <div className="card" style={{padding:34,textAlign:'center'}}><h3 className="card-title">{title}</h3><p className="muted">{body}</p><span className="badge">Connect an integration to unlock</span></div>}
