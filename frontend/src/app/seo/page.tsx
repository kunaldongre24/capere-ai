import { EmbeddedModule } from '@/components/embedded-module';
import { capereFetch, Envelope } from '@/lib/api';
import { SeoProjectControls } from '@/components/seo-project-controls';

const sections = [['Overview','overview'],['Website health','technical'],['Search keywords','keywords'],['Competitors','competitors'],['Local profile','gbp'],['Recommendations','recommendations'],['History','history']] as const;
type Metric={metric_name:string;metric_value:string;metric_date:string};
type Audit={id:string;status:string;score:number|null;issue_count:number|null;completed_at:string|null;started_at:string;site_url?:string;summary?:unknown};
type Integration={provider:string;status:string;last_sync_at:string|null;last_error:string|null};

const Icon=({name}:{name:string})=><span aria-hidden style={{fontSize:22}}>{({clicks:'↗',impressions:'◉',position:'⌖',health:'✓',audit:'⌕',keywords:'⌨',competitors:'◎',gbp:'★',recommendations:'✦',history:'◷'} as Record<string,string>)[name]??'•'}</span>;
const Card=({title,value,detail,icon}:{title:string;value:string;detail:string;icon:string})=><div className="card"><div style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><div className="eyebrow">{title}</div><Icon name={icon}/></div><div style={{fontSize:30,fontWeight:750,margin:'10px 0 4px'}}>{value}</div><p style={{margin:0}}>{detail}</p></div>;
const Bar=({label,value,max,detail}:{label:string;value:number;max:number;detail?:string})=><div style={{marginBottom:14}}><div style={{display:'flex',justifyContent:'space-between',gap:12,fontSize:13}}><span>{label}</span><strong>{detail??value}</strong></div><div style={{height:8,background:'var(--border)',borderRadius:99,overflow:'hidden',marginTop:6}}><div style={{width:`${Math.max(2,Math.min(100,max?value/max*100:0))}%`,height:'100%',background:'linear-gradient(90deg,#6d5dfc,#26c6a0)',borderRadius:99}}/></div></div>;
const Panel=({title,subtitle,children}:{title:string;subtitle:string;children:React.ReactNode})=><div className="card"><div style={{display:'flex',gap:10,alignItems:'center'}}><Icon name={title.toLowerCase().includes('keyword')?'keywords':title.toLowerCase().includes('technical')?'audit':'recommendations'}/><div><h3 style={{margin:0}}>{title}</h3><p style={{margin:'4px 0 0'}}>{subtitle}</p></div></div><div style={{marginTop:18}}>{children}</div></div>;

export default async function SeoPage({searchParams}:{searchParams:Promise<{view?:string}>}){
 const view=(await searchParams).view??'overview'; let summary:Envelope<Record<string,unknown>>|null=null;
 try{summary=await capereFetch('/api/v1/command-centers/seo-command-center/summary');}catch{}
 const d=summary?.data??{}; const metrics=(Array.isArray(d.metrics)?d.metrics:[]) as Metric[];
 const latest=(name:string)=>metrics.find(x=>x.metric_name===name)?.metric_value;
 const series=(name:string)=>metrics.filter(x=>x.metric_name===name).slice().reverse();
 const audit=(d.technicalAudit&&typeof d.technicalAudit==='object'?d.technicalAudit:null) as Audit|null;
 const findings=(Array.isArray(d.technicalFindings)?d.technicalFindings:[]) as Array<{code:string;title:string;severity:string;count:number;meaning:string;action:string}>;
 const tech=(d.technicalOverview&&typeof d.technicalOverview==='object'?d.technicalOverview:null) as null|{pagesCrawled:number;totalPages:number;crawlLimit:number;crawlStatus:string;crawlStopReason:string;crawlStartedAt:string|null;crawlEndedAt:string|null;internalLinks:number;externalLinks:number;brokenLinks:number;brokenResources:number;nonIndexablePages:number;duplicateContent:number;https:boolean;http2:boolean;sitemap:boolean;robotsTxt:boolean;certificateValid:boolean;certificateExpiresAt:string|null;ip:string|null};
 const history=(Array.isArray(d.auditHistory)?d.auditHistory:[]) as Audit[];
 const keywords=(Array.isArray(d.keywords)?d.keywords:[]) as Array<{keyword:string;rank:number|null;checked_on:string|null;url:string|null}>;
 const competitors=(Array.isArray(d.competitors)?d.competitors:[]) as Array<{domain:string;name:string|null;last_checked_at:string|null}>;
 const integrations=(Array.isArray(d.integrations)?d.integrations:[]) as Integration[];
 const recs=(Array.isArray(d.recommendations)?d.recommendations:[]) as Array<{id:string;title:string;action:string;priority:string;rationale?:string}>;
 const connected=(p:string)=>integrations.some(x=>x.provider===p&&x.status==='connected');
 const clicks=latest('clicks')??'0', impressions=latest('impressions')??'0', position=latest('position')??'—';
 const tabs=sections.map(([label,key])=>({label,href:key==='overview'?'/seo':`/seo?view=${key}`,active:view===key}));
 const score=audit?.score??0, issues=audit?.issue_count??0;
 const crawlDenominator=tech?Math.max(tech.pagesCrawled,Math.min(tech.totalPages,tech.crawlLimit)):0;
 let content:React.ReactNode;
 if(view==='technical') content=<div className="grid"><div className="grid grid-3"><Card title="Website health" value={`${score}/100`} detail="Overall health of your website" icon="health"/><Card title="Items to review" value={String(issues)} detail={issues?'Recommended improvements':'No issues found in this review'} icon="audit"/><Card title="Last review" value={audit?.completed_at?new Date(audit.completed_at).toLocaleDateString():'Not available'} detail={audit?.site_url??'Add your website'} icon="history"/></div><Panel title="What this means" subtitle="A simple summary of your website review."><p>{audit?'Your website is working well overall. We found a few improvements that can help search engines understand and load it more reliably. Review the recommendations below; a high score does not mean every individual check passed.':'A website review has not been completed yet.'}</p>{audit&&<><Bar label="Website health" value={score} max={100} detail={`${score} out of 100`}/><Bar label="Items to review" value={Math.min(issues,100)} max={100} detail={`${issues} items`}/></>}</Panel>{findings.length>0&&<Panel title="Recommended improvements" subtitle="Specific items found during the latest website review."><div className="grid grid-2">{findings.map(f=><div className="card" key={f.code} style={{borderLeft:`4px solid ${f.severity==='high'?'#ef5350':f.severity==='medium'?'#ffb020':'#6d5dfc'}`}}><div className="eyebrow">{f.severity==='high'?'Important':f.severity==='medium'?'Recommended':'Optional'} · {f.count} item</div><h3>{f.title}</h3><p>{f.meaning}</p><strong>What to do</strong><p>{f.action}</p></div>)}</div></Panel>}</div>;
 else if(view==='keywords') content=<div className="grid"><div className="grid grid-3"><Card title="Keywords found" value={String(keywords.length)} detail={keywords.length?'Search terms bringing visitors':'Search data is being collected'} icon="keywords"/><Card title="Average ranking" value={position} detail="Your average position in Google" icon="position"/><Card title="Times shown in search" value={impressions} detail="How often your site appeared" icon="impressions"/></div><Panel title="Search terms and rankings" subtitle="The words people use to find your business.">{keywords.length?<div>{keywords.map((k,i)=><Bar key={`${k.keyword}-${i}`} label={k.keyword} value={Math.max(0,101-(k.rank??100))} max={100} detail={k.rank?`Position ${k.rank}`:'Ranking not available'}/>)}</div>:<p>Search Console is connected and collecting data. So far, your website has appeared {impressions} time(s), received {clicks} click(s), and has an average ranking of {position}. Individual search terms will appear here after Google has collected more activity.</p>}</Panel></div>;
 else if(view==='competitors') content=<div className="grid"><div className="grid grid-3"><Card title="Competitors added" value={String(competitors.length)} detail="Businesses selected for comparison" icon="competitors"/><Card title="Your average ranking" value={position} detail="Current Google position" icon="position"/><Card title="Comparison status" value={competitors.length?'Ready':'Not set up'} detail="Add businesses you want to compare" icon="audit"/></div><Panel title="Competitor comparison" subtitle="See how your business compares with similar businesses in search.">{competitors.length?competitors.map(c=><div key={c.domain} style={{display:'flex',justifyContent:'space-between',padding:'12px 0',borderBottom:'1px solid var(--border)'}}><strong>{c.name??c.domain}</strong><span>{c.last_checked_at?`Checked ${new Date(c.last_checked_at).toLocaleDateString()}`:'Waiting for first comparison'}</span></div>):<p>Your website review is active, but no comparison businesses have been added. Add a few local or industry competitors to see search visibility differences and opportunities.</p>}</Panel></div>;
 else if(view==='gbp') content=<div className="grid"><div className="grid grid-3"><Card title="Google business listing" value={connected('google_business_profile')?'Connected':'Not connected'} detail="Your profile shown in Google Maps" icon="gbp"/><Card title="Customer reviews" value="—" detail="Available after connecting your listing" icon="gbp"/><Card title="Customer actions" value="—" detail="Calls, directions, and website visits" icon="clicks"/></div><Panel title="Local business visibility" subtitle="Understand how customers find and contact you through Google."><p>{connected('google_business_profile')?'Your Google Business Profile is connected. Reviews and customer activity will appear after the next data update.':'Connect your Google Business Profile to see reviews, calls, direction requests, website visits, and how customers discover your business locally.'}</p></Panel></div>;
 else if(view==='recommendations') content=<div className="grid"><div className="grid grid-3"><Card title="Recommended actions" value={String(recs.length||findings.length)} detail="Ways to improve your website" icon="recommendations"/><Card title="Website review" value={audit?'Complete':'Pending'} detail={audit?`${issues} items found`:'Waiting for first review'} icon="audit"/><Card title="Google visibility" value={`${impressions} appearances`} detail={`${clicks} visits from search`} icon="clicks"/></div>{recs.length?recs.map(r=><Panel key={r.id} title={r.title} subtitle={`${r.priority} priority`}><p>{r.rationale}</p><strong>What to do</strong><p>{r.action}</p></Panel>):findings.length?findings.map(f=><Panel key={f.code} title={f.title} subtitle={f.severity==='high'?'Important improvement':f.severity==='medium'?'Recommended improvement':'Optional improvement'}><p>{f.meaning}</p><strong>What to do</strong><p>{f.action}</p></Panel>):<Panel title="No urgent actions right now" subtitle="We will continue monitoring your website and search performance."><p>Recommendations appear when enough reliable information is available. Your connected services will continue collecting data automatically.</p></Panel>}</div>;
 else if(view==='history') content=<div className="grid"><div className="grid grid-3"><Card title="Audits completed" value={String(history.filter(x=>x.status==='succeeded').length)} detail="Recent retained runs" icon="history"/><Card title="Latest score" value={String(score)} detail="Technical health" icon="health"/><Card title="Latest issues" value={String(issues)} detail="Detected crawl issues" icon="audit"/></div><Panel title="Audit timeline" subtitle="Technical audit status and results over time.">{history.length?history.map(h=><div key={h.id} style={{display:'grid',gridTemplateColumns:'1fr auto auto',gap:16,padding:'14px 0',borderBottom:'1px solid var(--border)'}}><div><strong>{h.status==='succeeded'?'Completed audit':'Audit run'}</strong><div style={{fontSize:13,opacity:.7}}>{new Date(h.completed_at??h.started_at).toLocaleString()}</div></div><span>Score {h.score??'—'}</span><span>{h.issue_count??0} issues</span></div>):<p>No audit history is available.</p>}</Panel></div>;
 else {const rawClicks=series('clicks');const byDate=new Map<string,number>();for(const m of rawClicks){const date=new Date(m.metric_date).toLocaleDateString(undefined,{month:'short',day:'numeric'});byDate.set(date,(byDate.get(date)??0)+Number(m.metric_value));}const clickSeries=[...byDate.entries()];content=<div className="grid"><div className="grid grid-4"><Card title="Organic clicks" value={clicks} detail="Visits from Google Search" icon="clicks"/><Card title="Impressions" value={impressions} detail="Times shown in search" icon="impressions"/><Card title="Average position" value={position} detail="Average Google ranking" icon="position"/><Card title="Technical health" value={`${score}/100`} detail={audit?`${issues} findings · audit complete`:'Audit not completed'} icon="health"/></div>{tech&&<div className="grid grid-4"><Card title="Pages crawled" value={`${tech.pagesCrawled}/${crawlDenominator}`} detail={`Crawl ${tech.crawlStatus}`} icon="audit"/><Card title="Internal links" value={String(tech.internalLinks)} detail={`${tech.brokenLinks} broken links`} icon="clicks"/><Card title="Indexability" value={tech.nonIndexablePages?'Needs review':'Healthy'} detail={`${tech.nonIndexablePages} non-indexable pages`} icon="health"/><Card title="Site transport" value={tech.https&&tech.http2?'HTTPS + HTTP/2':'Review needed'} detail={tech.certificateValid?'Valid SSL certificate':'Certificate issue'} icon="health"/></div>}<div className="grid grid-2"><Panel title="Search performance" subtitle="Recent organic visibility from Search Console.">{clickSeries.length?clickSeries.map(([date,value])=><Bar key={date} label={date} value={value} max={Math.max(...clickSeries.map(x=>x[1]),1)} detail={`${value} clicks`}/>):<p>Search Console is connected but the property has very little historical traffic so far. Current totals are {clicks} clicks and {impressions} impression(s).</p>}</Panel><Panel title="Technical coverage" subtitle="Critical crawl and indexing signals from DataForSEO.">{tech?<><Bar label="SSL certificate" value={tech.certificateValid?100:0} max={100} detail={tech.certificateValid?'Valid':'Invalid'}/><Bar label="HTTPS" value={tech.https?100:0} max={100} detail={tech.https?'Enabled':'Missing'}/><Bar label="HTTP/2" value={tech.http2?100:0} max={100} detail={tech.http2?'Enabled':'Unavailable'}/><Bar label="XML sitemap" value={tech.sitemap?100:0} max={100} detail={tech.sitemap?'Detected':'Missing'}/><Bar label="robots.txt" value={tech.robotsTxt?100:0} max={100} detail={tech.robotsTxt?'Detected':'Missing'}/></>:<p>Run a technical audit to populate crawl coverage.</p>}</Panel></div>{tech&&<div className="grid grid-2"><Panel title="Crawl quality" subtitle="Content, resource, and link integrity."><Bar label="Broken links" value={tech.brokenLinks} max={Math.max(tech.pagesCrawled,1)} detail={String(tech.brokenLinks)}/><Bar label="Broken resources" value={tech.brokenResources} max={Math.max(tech.pagesCrawled,1)} detail={String(tech.brokenResources)}/><Bar label="Duplicate content" value={tech.duplicateContent} max={Math.max(tech.pagesCrawled,1)} detail={String(tech.duplicateContent)}/><Bar label="Non-indexable pages" value={tech.nonIndexablePages} max={Math.max(tech.pagesCrawled,1)} detail={String(tech.nonIndexablePages)}/></Panel><Panel title="Audit findings" subtitle="Priority improvements from the latest crawl.">{findings.map(f=><div key={f.code} style={{padding:'9px 0',borderBottom:'1px solid var(--border)',display:'flex',justifyContent:'space-between',gap:12}}><span>{f.title}</span><strong>{f.severity}</strong></div>)}</Panel></div>}<Panel title="SEO readiness" subtitle="Provider coverage across your command center."><Bar label="Search Console" value={connected('google_search_console')?100:0} max={100} detail={connected('google_search_console')?'Connected':'Not connected'}/><Bar label="Technical audit" value={audit?100:0} max={100} detail={audit?'Completed':'Pending'}/><Bar label="Google Business Profile" value={connected('google_business_profile')?100:0} max={100} detail={connected('google_business_profile')?'Connected':'Not connected'}/><Bar label="Competitor tracking" value={competitors.length?100:0} max={100} detail={competitors.length?'Active':'Not configured'}/></Panel><Panel title="Executive interpretation" subtitle="What the current SEO evidence says."><p>Google Search Console reports {impressions} impression(s), {clicks} click(s), and average position {position}. The DataForSEO crawl scored {score}/100 across {tech?.pagesCrawled??0} discovered page(s), with {issues} specific improvements. HTTPS, HTTP/2, and the SSL certificate are healthy; sitemap and robots.txt coverage are the clearest technical gaps. GBP and competitor tracking remain the largest missing data sources.</p></Panel></div>}
 return <EmbeddedModule product="SEO Command Center" title="Understand and improve your online visibility" description="A clear view of how customers find your business, what is working, and what to improve next." tabs={tabs}><SeoProjectControls projectId={typeof d.project==='object'&&d.project&&'id' in d.project?String((d.project as {id:string}).id):undefined} siteUrl={typeof d.project==='object'&&d.project&&'site_url' in d.project?String((d.project as {site_url:string}).site_url):undefined}/>{content}</EmbeddedModule>;
}
