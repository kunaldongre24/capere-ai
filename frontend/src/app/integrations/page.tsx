import { Page } from '@/components/page';
const providers=[['GoHighLevel','CRM, leads, opportunities, conversations, workflows'],['Google Analytics 4','Traffic, conversions, events, and revenue'],['Search Console','Queries, clicks, impressions, CTR, position'],['Google Business Profile','Reviews, calls, photos, and directions'],['DataForSEO','Keywords, SERPs, competitors, backlinks'],['GitHub','Repository analysis and approved changes']];
export default function IntegrationsPage(){return <Page eyebrow="Integrations" title="Connect your growth data" subtitle="Capere reads intelligence from your existing systems; GoHighLevel remains your CRM."><div className="grid grid-3">{providers.map(([name,desc])=><div className="card" key={name}><h2 className="card-title">{name}</h2><p className="muted" style={{minHeight:45}}>{desc}</p><span className="badge">Connection status available in API</span></div>)}</div></Page>}
