import { Page, EmptyState } from '@/components/page'; export default function Knowledge(){return <Page eyebrow="Knowledge system" title="Your firm's operating memory" subtitle="CPA playbooks, SOPs, and implementation guides with citation-ready retrieval."><EmptyState title="No documents ingested" body="Upload your playbook and SOPs through the API to make them available to Capere agents."/></Page>}
