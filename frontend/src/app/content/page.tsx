import { Page, EmptyState } from '@/components/page'; export default function Content(){return <Page eyebrow="Content studio" title="Create content that compounds" subtitle="Draft blogs, GBP posts, emails, and metadata with your firm's knowledge context."><EmptyState title="Connect your knowledge base" body="Content generation becomes available after documents are ingested and embeddings are ready."/></Page>}
