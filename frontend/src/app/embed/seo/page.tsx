// GoHighLevel custom-menu entry point. Keep this as a thin alias so the
// embedded experience and the normal Capere route use exactly the same data
// fetching and UI code.
export { default } from '@/app/seo/page';
