import { login } from './actions';
export default async function LoginPage({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <div className="auth-page"><div className="card auth-card"><div style={{display:'flex',alignItems:'center',marginBottom:28}}><div className="brand-mark">C</div><div className="brand">Capere <span>AI</span></div></div><div className="eyebrow">Secure workspace</div><h1 className="title">Welcome back</h1><p className="subtitle">Sign in to your firm's growth operating system.</p>{p.error&&<p className="form-error">Sign in failed. Check your credentials.</p>}<form action={login} className="grid"><label>Email<input name="email" type="email" required style={input}/></label><label>Password<input name="password" type="password" required style={input}/></label><button className="btn" type="submit">Sign in</button></form></div></div>}
const input:React.CSSProperties={display:'block',width:'100%',marginTop:7,padding:'11px 12px',border:'1px solid var(--line)',borderRadius:8,font:'inherit'};
