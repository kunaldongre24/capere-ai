import { Page, EmptyState } from '@/components/page'; export default function Jobs(){return <Page eyebrow="Operations" title="Job activity" subtitle="Monitor synchronization, website reviews, content processing, and scheduled reports."><div className="grid grid-4"><div className="card stat-card"><div className="metric-label">Running</div><div className="metric">0</div><span className="muted">No jobs in progress</span></div><div className="card stat-card"><div className="metric-label">Completed today</div><div className="metric">0</div><span className="muted">Completed background work</span></div><div className="card stat-card"><div className="metric-label">Waiting</div><div className="metric">0</div><span className="muted">Queued jobs</span></div><div className="card stat-card"><div className="metric-label">Failed</div><div className="metric">0</div><span className="muted">No errors reported</span></div></div><EmptyState title="No recent background activity" body="Scheduled workers will populate this view as connected services synchronize and reports are generated." badge="All systems are quiet"/></Page>}
